// Returns null — frontend rendering is handled by render.php (server-side).
// This avoids "block validation errors" if the form markup changes later.
export default function save() {
	return null;
}
